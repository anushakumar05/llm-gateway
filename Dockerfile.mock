FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY mock_provider/ ./mock_provider/
CMD ["uvicorn", "mock_provider.main:app", "--host", "0.0.0.0", "--port", "9001"]